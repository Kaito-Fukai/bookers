<h2>編集画面</h2>

<%= form_for(@post) do |f| %>
	<h4> 書籍名 </h4>
	<%= f.text_field :title %>

	<h4> 感想 </h4>
	<%= f.text_area :impression %>

	<h4> 投稿者名 </h4>
	<%= f.text_field :name %>

	<%= f.submit '編集完了' %>

<% end %>
